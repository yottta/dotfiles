#!/bin/bash

[ -f ~/.profile ] && . ~/.profile
[ -f ~/.aliases ] && . ~/.aliases
[ -f /etc/bashrc ] && . /etc/bashrc
[ -f ~/.common_exports ] && . ~/.common_exports
[ -f ~/.exports ] && . ~/.exports

